import { OverflowDetector } from './overflow-detector';

describe('OverflowDetector', () => {
   it('should create an instance', () => {
      const directive = new OverflowDetector();
      expect(directive).toBeTruthy();
   });
});
