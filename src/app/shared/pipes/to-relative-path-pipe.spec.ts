import { ToRelativePathPipe } from './to-relative-path.pipe';

describe('ToRelativePathPipe', () => {
  it('create an instance', () => {
    const pipe = new ToRelativePathPipe();
    expect(pipe).toBeTruthy();
  });
});
